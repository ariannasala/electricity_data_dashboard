# Streamlit electricity data dashboard

A dashboard that takes and visualizes data from the ORACLE database. 

## Run the dashboard locally

Start the Streamlit dashboard with:

```bash
docker compose up dashboard
```

Then open:

```text
http://localhost:8051
```
